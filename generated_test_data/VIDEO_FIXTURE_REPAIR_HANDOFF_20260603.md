# Generated Test Data Video Fixture Repair Handoff

Estonian timestamp: 2026-06-03

## Purpose

This report is a handoff for another AI or engineer to repair the `generated_test_data` video-fixture mismatch, generate the missing Apple-style video files if needed, and verify the repaired dataset state.

This document is not proof that the fix has been completed. It records the verified current mismatch and a constrained repair target.

## Classification

- Primary type: test-data documentation and repair handoff
- Verified: current filesystem state, current `README.md`, current `manifest.json`, and repository search results checked in this task
- Inferred: likely cause and safest repair order
- Unverified: the exact generator implementation, because no generator script was found in the current repository checkout

## Verified Current State

### Verified paths

- `generated_test_data/` exists.
- `generated_test_data/README.md` exists.
- `generated_test_data/manifest.json` exists.

### Verified README-to-filesystem mismatch

The dataset `README.md` explicitly names 24 file paths in code spans/tables for `manifest.json` and video fixtures.

- 19 of those 24 named files exist.
- 5 named files do not exist:
  - `videos_no_gps/apple_like_h264_mov_no_gps.mov`
  - `videos_no_gps/apple_like_h264_mp4_no_gps.mp4`
  - `videos_with_gps/apple_like_h264_mov_gps_tallinn.mov`
  - `videos_with_gps/apple_like_h264_mov_gps_tokyo.mov`
  - `videos_with_gps/apple_like_h264_mp4_gps_new_york.mp4`

### Verified blocking anomaly

At dataset root, these two paths currently exist as zero-byte files, not directories:

- `generated_test_data/videos_no_gps`
- `generated_test_data/videos_with_gps`

Because they are files, the five README-listed child paths under them cannot exist.

### Verified manifest mismatch

`generated_test_data/manifest.json` currently claims:

- `summary.videos_with_gps = 3`
- `summary.videos_no_gps = 2`

It also contains record paths for these five missing files:

- `videos_with_gps\\apple_like_h264_mov_gps_tallinn.mov`
- `videos_with_gps\\apple_like_h264_mov_gps_tokyo.mov`
- `videos_with_gps\\apple_like_h264_mp4_gps_new_york.mp4`
- `videos_no_gps\\apple_like_h264_mov_no_gps.mov`
- `videos_no_gps\\apple_like_h264_mp4_no_gps.mp4`

So the current manifest and README agree with each other, but they disagree with the current filesystem.

### Verified generator-script gap

Repository search in this task did not find a checked-in generator script matching the README claim:

- README says dataset was generated by `generate_photo_frame_test_dataset.py`
- Current repo search did not find that script

This means the next AI should not assume the generator exists locally in the current checkout.

## Additional Verified Context

- The other video fixtures referenced by the README do exist under:
  - `corrupted/`
  - `duplicates/`
  - `gps_same_location/`
  - `gps_valid/`
  - `invalid_gps/`
  - `mixed_batch/`
  - `no_gps/`
  - `rotated/`
- There is a separate `generated_test_data/heic` zero-byte file at dataset root. That anomaly was verified but is outside the main video-fixture repair scope unless the next AI chooses to reconcile all README placeholder/folder semantics.
- `manifest.json.arguments.include_videos` is currently `false` even though the manifest also includes video summaries and video records. That inconsistency is verified and should be treated as a separate metadata issue to reconcile during the repair.

## Safest Repair Goal

Repair the dataset so that the current filesystem, `README.md`, and `manifest.json` all agree without changing unrelated existing fixture folders or file names.

Minimum successful end state:

1. `generated_test_data/videos_with_gps` is a directory, not a file.
2. `generated_test_data/videos_no_gps` is a directory, not a file.
3. The five README/manifest-listed Apple-style video files exist at the exact relative paths listed above.
4. The repaired files are non-empty.
5. `README.md` and `manifest.json` accurately describe the repaired state.
6. If the exact original generation path cannot be reconstructed, the replacement files still preserve the documented proof semantics:
   - GPS-tagged videos in `videos_with_gps/`
   - no-GPS videos in `videos_no_gps/`
   - Apple-style `.mov` / `.mp4` containers where documented
   - graceful parser-proof scope only, not real iPhone capture claims

## Inferred Likely Cause

The most likely direct cause is a path collision or incomplete repair where placeholder zero-byte files were left at:

- `generated_test_data/videos_with_gps`
- `generated_test_data/videos_no_gps`

instead of creating directories with the five child video fixtures.

This is an inference, not a verified history claim.

## Constraints For The Next AI

- Preserve existing fixture names and folder names outside this mismatch unless direct verification proves another mismatch.
- Do not silently rewrite unrelated manifest records.
- Do not claim real-device provenance for the Apple-style video fixtures.
- Prefer the smallest repair that restores agreement between filesystem, README, and manifest.
- If the exact original generator cannot be found, either:
  - reconstruct the missing files directly with a small deterministic script, or
  - regenerate only the missing fixture subset with `ffmpeg` plus metadata tooling, then verify outputs

## Recommended Repair Order

1. Back up or record the current zero-byte file state for `videos_with_gps` and `videos_no_gps`.
2. Remove or rename only those two blocking zero-byte files.
3. Create directories with the same names.
4. Generate the five missing video fixtures with the exact documented relative paths.
5. Verify file existence and non-zero size.
6. Verify container type, duration, codec family, resolution, audio presence, and GPS/no-GPS semantics with tooling such as `ffprobe`, `exiftool`, or equivalent.
7. Reconcile `manifest.json` if any generated metadata values differ from current claims.
8. Update `generated_test_data/README.md` only if the actual repaired outputs differ from the currently documented filenames or semantics.

## Exact Files To Generate Or Repair

### GPS-tagged Apple-style video fixtures

- `generated_test_data/videos_with_gps/apple_like_h264_mov_gps_tallinn.mov`
- `generated_test_data/videos_with_gps/apple_like_h264_mov_gps_tokyo.mov`
- `generated_test_data/videos_with_gps/apple_like_h264_mp4_gps_new_york.mp4`

### No-GPS Apple-style video fixtures

- `generated_test_data/videos_no_gps/apple_like_h264_mov_no_gps.mov`
- `generated_test_data/videos_no_gps/apple_like_h264_mp4_no_gps.mp4`

## Verification Checklist For The Next AI

The next AI should not stop at file creation. It should verify:

1. Path existence
   - All five files exist.
   - `videos_with_gps` and `videos_no_gps` are directories, not files.
2. Basic file sanity
   - All five files are non-empty.
3. Media/container sanity
   - `.mov` files report QuickTime-style container metadata.
   - `.mp4` files report MP4 container metadata.
   - Duration is close to the documented 2.00 seconds.
   - Resolution is close to the documented 640x360.
   - Video codec family is H.264 / `avc1` or a justified equivalent if documentation is updated.
   - Audio track presence matches the documented expectation.
4. GPS semantics
   - `videos_with_gps/*` expose location metadata when inspected with the chosen tooling, or the next AI updates docs/manifest honestly if the chosen tooling cannot embed the exact expected GPS format.
   - `videos_no_gps/*` do not expose location metadata.
5. Documentation/metadata alignment
   - `README.md` file table matches actual paths.
   - `manifest.json` summary counts and record paths match actual paths.

## Suggested Commands The Next AI Can Adapt

```powershell
Test-Path -LiteralPath 'S:\PF_login\generated_test_data\videos_with_gps\apple_like_h264_mov_gps_tallinn.mov'
Get-Item -LiteralPath 'S:\PF_login\generated_test_data\videos_with_gps'
Get-Item -LiteralPath 'S:\PF_login\generated_test_data\videos_no_gps'
Get-ChildItem -LiteralPath 'S:\PF_login\generated_test_data\videos_with_gps'
Get-ChildItem -LiteralPath 'S:\PF_login\generated_test_data\videos_no_gps'
```

```powershell
ffprobe -hide_banner -show_streams -show_format "S:\PF_login\generated_test_data\videos_with_gps\apple_like_h264_mov_gps_tallinn.mov"
ffprobe -hide_banner -show_streams -show_format "S:\PF_login\generated_test_data\videos_no_gps\apple_like_h264_mp4_no_gps.mp4"
```

## Verified Evidence Basis For This Handoff

This handoff was prepared from direct checks of:

- `generated_test_data/README.md`
- `generated_test_data/manifest.json`
- recursive filesystem inventory under `generated_test_data/`
- repository text search for the named dataset generator script

No runtime playback proof, parser proof, or actual regeneration was performed in this handoff step.
