# Geocode Dry-Run From Sample OpenSpec

Status: proof-enabler for real GPS/geocode gate.

The dry-run uses sample-shaped GPS metadata and provider selection without making a live provider call by default. Shared proof artifacts should redact exact private coordinates and exact addresses unless explicitly approved by the operator.
