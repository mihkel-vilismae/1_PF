# Real iCloud Listing Preflight OpenSpec

Status: safe bridge toward real iCloud media source proof.

The preflight may verify explicit operator opt-in and iCloudPD session-path readiness. It must not download media by default. It may describe a listing/count plan and later collect sanitized count/exit-code metadata. It must not collect credentials, cookies, tokens, session file contents, or full private filenames by default.
