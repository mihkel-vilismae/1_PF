# Terminal Demo Logs Detail Panel Proof

Command:

```bash
npm run proof:terminal-demo-logs-detail-panel
```

This proof renders View `L` with a multi-line action JSONL fixture and verifies the selected detail panel shows path, role, kind, status, file size, line count, purpose, reader message, and bounded tail/preview content.
