# Terminal Demo Logs View Overview Proof

Command:

```bash
npm run proof:terminal-demo-logs-view-overview
```

This proof renders View `L` against isolated runtime fixture files and verifies the overview table shows all seven registry entries, runtime paths, file kind, size, line count, and snapshot states.
