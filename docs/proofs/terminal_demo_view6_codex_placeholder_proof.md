# Terminal Demo View 6 Codex Placeholder Proof

## Command

```bash
npm run proof:terminal-demo-view6-codex-placeholder
```

## Proves

- Each enabled View 6 fixture button opens `VIEW 6 CODEX PLACEHOLDER MODAL`.
- The modal includes `this will be done by Codex`.
- JSONL evidence records `CODEX_DEFERRED`.
- JSONL evidence records `launchesPlayback=false`.
- JSONL evidence uses `branchFeature: view6_fixture_playback`.

## Non-Claims

No browser playback, fullscreen playback, address-overlay playback, worker, queue execution, auth, DB write, cron, or hardware behavior is proven.
