# Deprecated and Superseded Documentation Log

Status: Slice 1 candidate log.  
Created: 2026-04-26 19:47 EEST.

## Rule

This log does **not** permanently delete or move any file. It records documents that are likely deprecated, superseded, historical, or not safe as current authority until harvested and reconciled.

## Candidate table

| Path | Current marker | Reason | Required next action before deletion/relocation |
|---|---|---|---|
| `docs/OLD_DOCS/00_TABLE_OF_CONTENTS.md` | Superseded candidate | Old documentation area; useful historical index/evidence but superseded for current authority. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `docs/OLD_DOCS/01_SYSTEM_OVERVIEW.md` | Superseded candidate | Old documentation content likely harvested/superseded by active workflow and future vision/spec docs; keep archived for now. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `docs/OLD_DOCS/02_SYSTEM_INVARIANTS.md` | Superseded candidate | Old documentation content likely harvested/superseded by active workflow and future vision/spec docs; keep archived for now. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `docs/OLD_DOCS/03_ARCHITECTURE.md` | Superseded candidate | Old documentation content likely harvested/superseded by active workflow and future vision/spec docs; keep archived for now. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `docs/OLD_DOCS/04_SINGLE_SOURCE_OF_TRUTH.md` | Superseded candidate | Old documentation content likely harvested/superseded by active workflow and future vision/spec docs; keep archived for now. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `docs/OLD_DOCS/05_STATE_MACHINE.md` | Superseded candidate | Old documentation content likely harvested/superseded by active workflow and future vision/spec docs; keep archived for now. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `docs/OLD_DOCS/06_DATABASE_SCHEMA.md` | Superseded candidate | Old documentation content likely harvested/superseded by active workflow and future vision/spec docs; keep archived for now. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `docs/OLD_DOCS/07_PIPELINE_STAGES.md` | Superseded candidate | Old documentation content likely harvested/superseded by active workflow and future vision/spec docs; keep archived for now. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `docs/OLD_DOCS/08_WORKERS_AND_OWNERSHIP.md` | Superseded candidate | Old documentation content likely harvested/superseded by active workflow and future vision/spec docs; keep archived for now. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `docs/OLD_DOCS/09_CRON_AND_WATCHDOG.md` | Superseded candidate | Old documentation content likely harvested/superseded by active workflow and future vision/spec docs; keep archived for now. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `docs/OLD_DOCS/10_CONCURRENCY_AND_LOCKING.md` | Superseded candidate | Old documentation content likely harvested/superseded by active workflow and future vision/spec docs; keep archived for now. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `docs/OLD_DOCS/11_LOGGING_AND_EVENT_MODEL.md` | Superseded candidate | Old documentation content likely harvested/superseded by active workflow and future vision/spec docs; keep archived for now. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `docs/OLD_DOCS/12_STATE_AND_RECOVERY.md` | Superseded candidate | Old documentation content likely harvested/superseded by active workflow and future vision/spec docs; keep archived for now. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `docs/OLD_DOCS/13_FRONTEND_BACKEND_CONTRACT.md` | Superseded candidate | Old documentation content likely harvested/superseded by active workflow and future vision/spec docs; keep archived for now. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `docs/OLD_DOCS/14_VERSIONING_AND_CHANGELOG_RULES.md` | Superseded candidate | Old documentation content likely harvested/superseded by active workflow and future vision/spec docs; keep archived for now. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `docs/OLD_DOCS/15_CURRENT_IMPLEMENTATION_STATUS.md` | Superseded candidate | Old documentation content likely harvested/superseded by active workflow and future vision/spec docs; keep archived for now. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `docs/OLD_DOCS/16_DOCUMENTATION_RECONCILIATION_REPORT.md` | Superseded candidate | Old documentation content likely harvested/superseded by active workflow and future vision/spec docs; keep archived for now. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `docs/OLD_DOCS/17_REPO_ANALYSIS_AND_DOC_UPDATE_PROMPT.md` | Superseded candidate | Old documentation content likely harvested/superseded by active workflow and future vision/spec docs; keep archived for now. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `docs/OLD_DOCS/18_CANONICAL_BACKEND_CONTRACT_SET.md` | Superseded candidate | Old documentation content likely harvested/superseded by active workflow and future vision/spec docs; keep archived for now. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `docs/OLD_DOCS/19_BACKEND_RUNTIME_CONTRACT.md` | Superseded candidate | Old documentation content likely harvested/superseded by active workflow and future vision/spec docs; keep archived for now. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `docs/OLD_DOCS/20_STATE_AND_TRUTH_CONTRACT.md` | Superseded candidate | Old documentation content likely harvested/superseded by active workflow and future vision/spec docs; keep archived for now. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `docs/OLD_DOCS/21_EXECUTION_AND_RECOVERY_CONTRACT.md` | Superseded candidate | Old documentation content likely harvested/superseded by active workflow and future vision/spec docs; keep archived for now. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `docs/OLD_DOCS/22_ACCEPTANCE_AND_VALIDATION_CONTRACT.md` | Superseded candidate | Old documentation content likely harvested/superseded by active workflow and future vision/spec docs; keep archived for now. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `docs/OLD_DOCS/23_VIEW_A_INIT_RECONCILIATION_PROMPT.md` | Superseded candidate | Old documentation content likely harvested/superseded by active workflow and future vision/spec docs; keep archived for now. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `docs/OLD_DOCS/DASHBOARD_OVERVIEW.md` | Superseded candidate | Old documentation content likely harvested/superseded by active workflow and future vision/spec docs; keep archived for now. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `docs/OLD_DOCS/VIEW_A_INIT.md` | Superseded candidate | Old documentation content likely harvested/superseded by active workflow and future vision/spec docs; keep archived for now. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `docs/OLD_DOCS/VIEW_B_TEST.md` | Superseded candidate | Old documentation content likely harvested/superseded by active workflow and future vision/spec docs; keep archived for now. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `docs/OLD_DOCS/VIEW_C_LAST_RUN_INFO.md` | Superseded candidate | Old documentation content likely harvested/superseded by active workflow and future vision/spec docs; keep archived for now. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `docs/OLD_DOCS/VIEW_D_RUNNING_PROCESS.md` | Superseded candidate | Old documentation content likely harvested/superseded by active workflow and future vision/spec docs; keep archived for now. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `docs/OLD_DOCS/VIEW_E_DATABASE_VIEWER.md` | Superseded candidate | Old documentation content likely harvested/superseded by active workflow and future vision/spec docs; keep archived for now. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `docs/OLD_DOCS/issues_errors_discrepancies.md` | Superseded candidate | Old documentation area; useful historical index/evidence but superseded for current authority. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `task_docs/2026-04-20_dashboard-transit-terminal.md` | Historical task reference | Task implementation notes; preserve as historical task evidence, not current spec authority. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `task_docs/2026-04-20_explain-controls-inspect-mode.md` | Historical task reference | Task implementation notes; preserve as historical task evidence, not current spec authority. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `task_docs/2026-04-20_explain-values-source-mode.md` | Historical task reference | Task implementation notes; preserve as historical task evidence, not current spec authority. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `task_docs/2026-04-20_runtime-backend-foundation.md` | Historical task reference | Task implementation notes; preserve as historical task evidence, not current spec authority. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `task_docs/2026-04-20_show-backend-status-mode.md` | Historical task reference | Task implementation notes; preserve as historical task evidence, not current spec authority. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `task_docs/2026-04-20_show-real-vs-mock-mode.md` | Historical task reference | Task implementation notes; preserve as historical task evidence, not current spec authority. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `task_docs/2026-04-20_view-e-database-viewer.md` | Historical task reference | Task implementation notes; preserve as historical task evidence, not current spec authority. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `task_docs/README.md` | Historical task reference | Task implementation notes; preserve as historical task evidence, not current spec authority. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |
| `task_docs/_TABLE_OF_CONTENTS.md` | Historical task reference | Task implementation notes; preserve as historical task evidence, not current spec authority. | Harvest useful content into `docs/vision_and_implementation/` or a source-of-truth doc; then move to `docs/docs_parsed/` or `docs/to_be_deleted/` with an explicit marker in a later slice/workflow. |

## Immediate Slice 1 decisions

- No documentation files were moved.
- No documentation files were permanently deleted.
- `docs/OLD_DOCS/` remains historical evidence, not current authority by default.
- `task_docs/` remains task-history evidence, not current authority by default.

## Slice 2 harvesting note — 2026-04-26 19:59 EEST

Slice 2 harvested current-useful concepts from authoritative, supporting, historical, and superseded candidate documents into:

- `PROJECT_VISION.md`
- `CURRENT_IMPLEMENTATION_SPEC.md`
- `DASHBOARD_VIEWS_SPEC.md`

No documentation files were moved or deleted in Slice 2.

The following candidate groups remain marked for later reconciliation rather than immediate relocation:

| Candidate group | Slice 2 status | Next handling |
|---|---|---|
| `docs/OLD_DOCS/` | Superseded or historical candidate group | Harvest remaining architecture, worker, state, recovery, and acceptance content in Slice 3 before deciding `docs/docs_parsed/` vs `docs/to_be_deleted/`. |
| `task_docs/` | Historical task evidence | Keep as implementation history unless later fully harvested and explicitly reclassified. |
| Auth-specific docs under `docs/` | Supporting authority candidates | Consolidate into `AUTH_AND_2FA_SPEC.md` in Slice 3. |
| Button/view evidence docs | Supporting implementation evidence | Keep available for dashboard verification; decide after Slice 3 whether they remain active working references. |

A document listed here is not approved for deletion merely because it is old. It must be harvested, reconciled, and explicitly classified before relocation.
