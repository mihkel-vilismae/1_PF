interface:
  display_name: "New Auth Login Monitor"
  short_description: "Observe NEW AUTH login logs and session metadata safely."
  default_prompt: "Use this skill to monitor NEW AUTH login attempts, sanitized logs, and iCloudPD cookie metadata without exposing secrets."
