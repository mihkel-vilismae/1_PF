interface:
  display_name: "New Auth Login Monitor"
  short_description: "Observe NEW AUTH login logs and session metadata safely."
  default_prompt: "Use $new-auth-login-monitor to monitor NEW AUTH attempts, sanitized logs, and iCloudPD session metadata safely."
